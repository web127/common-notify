# AI 使用说明

## AI 帮助的关键部分

1. **需求分析与架构设计**
   - AI 帮助梳理了需求边界，明确了哪些功能应该包含在 MVP 中
   - 提供了初始的分层架构思路（API层、服务层、仓库层）
   - 帮助确定了使用 SQLite 作为轻量级持久化方案

2. **代码结构搭建**
   - AI 协助设计了清晰的目录结构（internal/model, repository, service, api, worker）
   - 提供了 RESTful API 的路由设计建议
   - 帮助编写了基础的 HTTP 处理器样板代码

3. **数据模型设计**
   - AI 提供了 providers 和 notifications 表的初始字段建议
   - 帮助设计了 StringMap 自定义类型来处理 JSON 格式的 headers
   - 建议了合适的索引（status + next_send_at）来优化查询性能

4. **重试逻辑实现**
   - AI 帮助设计了状态机流转（pending → sending → success/failed）
   - 提供了固定间隔重试的实现思路

## 未采纳的 AI 建议

1. **引入分布式消息队列（Kafka/RabbitMQ）**
   - **未采纳原因**：当前需求是中等量级，SQLite + Worker 轮询已足够。引入 MQ 会增加运维复杂度，与 MVP 目标不符。
   - **决策依据**：根据设计要求中的"避免过度设计"原则，第一版应聚焦核心功能。

2. **实现复杂的 Body 模板渲染引擎**
   - **未采纳原因**：需求中没有明确要求动态模板渲染，业务系统可以直接渲染好 Body 透传。
   - **决策依据**：保持系统简洁，YAGNI（You Aren't Gonna Need It）原则。

3. **通知去重与幂等性保证**
   - **未采纳原因**：增加了系统复杂度，且可以由下游系统处理。
   - **决策依据**：需求中业务系统不关心返回值，隐含了下游应具备幂等处理能力。

4. **多实例部署与分布式锁**
   - **未采纳原因**：MVP 阶段不需要高可用架构，单实例已满足需求。
   - **决策依据**：先验证核心功能，未来可以演进。

## 自主做出的关键决策

1. **选择 SQLite 而非 PostgreSQL/MySQL**
   - **原因**：零配置、开箱即用，适合中等量级和快速验证；无需额外的数据库运维。

2. **Worker 使用简单轮询而非复杂调度**
   - **原因**：实现简单，易于理解和维护；对于秒级调度，轮询已足够高效。

3. **HTTP 状态码判断：2xx 视为成功**
   - **原因**：符合 HTTP 标准语义，其他状态码（包括 3xx 重定向）都视为失败进行重试。

4. **保留最终失败的通知**
   - **原因**：需求明确要求保留多次重试失败的通知，支持后续人工介入或手动重试。

5. **请求头合并策略：通知级别覆盖供应商级别**
   - **原因**：提供灵活性，允许单个通知覆盖默认配置。

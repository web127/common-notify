# Common-Notify 设计文档

## 1. 问题理解

### 1.1 需求背景
企业内部多个业务系统需要在关键事件发生时调用外部供应商的 HTTP/HTTPS API 进行通知。不同供应商的 API 格式（URL、Header、Body）各不相同，业务系统只需要确保通知能够可靠送达，不需要处理返回值。

### 1.2 系统边界

**本系统解决的问题：**
- 接收业务系统的通知请求并持久化
- 管理不同供应商的 API 配置（URL、Header、Body 模板）
- 可靠地将通知投递到目标 HTTP/HTTPS 端点
- 固定间隔重试机制，确保"至少一次"投递语义
- 保留最终失败的通知，支持手动重试

**本系统明确不解决的问题：**
- 不支持除 HTTP/HTTPS 以外的通知协议（如消息队列、邮件、短信等）
- 不实现复杂的动态 Body 模板渲染（仅支持透传）
- 不实现通知去重或幂等性保证（由上游业务系统或下游接收方处理）
- 不实现多租户/权限控制（假设为内部可信环境）
- 不实现复杂的监控告警系统（仅提供基础状态查询）

**取舍原因：**
作为第一版 MVP 系统，我们聚焦于核心的"可靠 HTTP 投递"功能。其他功能可以在未来按需添加，避免过度设计。

---

## 2. 整体架构与核心设计

### 2.1 架构图

```
┌─────────────────┐
│   业务系统      │
└────────┬────────┘
         │ HTTP API
         ▼
┌─────────────────────────────────────────────┐
│              common-notify                   │
│  ┌─────────────────────────────────────────┐│
│  │         API Layer (Gorilla Mux)         ││
│  └──────────┬──────────────────────┬───────┘│
│             │                      │        │
│             ▼                      ▼        │
│  ┌──────────────────┐   ┌─────────────────┐ │
│  │ Provider Service │   │  Notif Service  │ │
│  └────────┬─────────┘   └────────┬────────┘ │
│           │                        │          │
│           └──────────┬─────────────┘          │
│                      ▼                        │
│  ┌─────────────────────────────────────────┐ │
│  │         Repository Layer                 │ │
│  │  ┌──────────────┐      ┌──────────────┐│ │
│  │  │ ProviderRepo │      │ NotifRepo    ││ │
│  │  └──────┬───────┘      └──────┬───────┘│ │
│  └─────────┼───────────────────────┼────────┘ │
│            ▼                       ▼          │
│  ┌─────────────────────────────────────────┐ │
│  │           SQLite Database                │ │
│  │  - providers (供应商配置表)              │ │
│  │  - notifications (通知任务表)            │ │
│  └─────────────────────────────────────────┘ │
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │           Worker (后台调度)              │ │
│  │  - 定时扫描待发送通知                    │ │
│  │  - 执行 HTTP 投递                        │ │
│  │  - 处理重试逻辑                          │ │
│  └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
         │ HTTP/HTTPS
         ▼
┌─────────────────┐
│  外部供应商 API  │
└─────────────────┘
```

### 2.2 核心模块

| 模块 | 职责 |
|-----|-----|
| **API Layer** | 提供 RESTful API，处理供应商配置管理和通知接收 |
| **Service Layer** | 业务逻辑：配置验证、通知合并、HTTP 投递、重试策略 |
| **Repository Layer** | 数据访问抽象 |
| **SQLite** | 轻量级持久化存储 |
| **Worker** | 后台定时任务，处理通知投递 |

### 2.3 数据模型

**providers 表（供应商配置）**
- `id`: 主键
- `name`: 供应商名称（唯一）
- `base_url`: 基础 URL
- `method`: HTTP 方法（默认 POST）
- `headers`: 请求头（JSON）
- `body_tpl`: Body 模板（预留）
- `created_at`, `updated_at`: 时间戳

**notifications 表（通知任务）**
- `id`: 主键
- `provider_id`: 关联供应商
- `idempotency_key`: 幂等键（唯一，用于去重）
- `event_type`: 事件类型
- `url`, `method`, `headers`, `body`: 请求参数（可覆盖供应商配置）
- `status`: 状态（pending/sending/success/failed）
- `retry_count`: 已重试次数
- `max_retry`: 最大重试次数
- `retry_interval`: 重试间隔（秒）
- `next_send_at`: 下次发送时间
- `last_error`: 上次错误信息
- `created_at`, `updated_at`: 时间戳

索引：
- `(status, next_send_at)`: 用于快速查询待发送通知
- `(provider_id)`: 供应商查询
- `(idempotency_key)`: 幂等键唯一索引（仅非空值）

---

## 3. 可靠性与失败处理

### 3.1 投递语义

选择 **"至少一次"（at-least-once）** 投递语义。

**依据：**
- 业务系统需求明确表示"只需确保通知请求能够被稳定、可靠地送达"
- 业务系统不需要关心返回值，说明下游系统应当具备处理重复通知的能力

### 3.2 失败处理策略

| 场景 | 处理策略 |
|-----|---------|
| **HTTP 请求失败** | 记录错误，增加重试次数，固定间隔后重试 |
| **非 2xx 状态码** | 视为失败，同上重试 |
| **外部系统长期不可用** | 持续重试直到达到最大重试次数，然后标记为 `failed` |
| **达到最大重试次数** | 标记为 `failed` 状态，保留数据，支持手动重试 |

### 3.3 固定间隔重试策略

- 默认重试间隔：60 秒
- 默认最大重试次数：5 次
- 可在创建通知时自定义

---

## 4. 取舍与演进

### 4.1 不采纳的"过度设计"

在 AI 方案建议中（假设存在），以下设计被认为是过度的并选择不采纳：

| 不采纳的设计 | 原因 |
|-------------|------|
| 分布式消息队列（如 Kafka/RabbitMQ） | 中等量级流量下，SQLite + 轮询已足够；引入 MQ 增加运维复杂度 |
| 分布式部署/高可用架构 | MVP 阶段不需要；单实例 + SQLite 已能满足需求 |
| 复杂的 Body 模板引擎 | 需求中未明确要求；业务系统可以直接渲染好 Body 透传 |
| 内置监控/告警系统 | 超出核心范围；可对接外部监控系统 |

### 4.2 未来演进方向

当流量或复杂度显著增长时，可以按以下方向演进：

**阶段 1：性能优化**
- 替换 SQLite 为 PostgreSQL/MySQL
- 增加 Worker 数量（并发投递）
- 添加连接池和 HTTP 客户端优化

**阶段 2：高可用**
- 引入分布式锁（如 Redis）避免重复投递
- 支持多实例部署
- 数据库主从复制

**阶段 3：功能增强**
- 引入消息队列（削峰填谷）
- 支持 Body 模板渲染
- 通知去重/幂等性保证
- 丰富的监控指标和告警

---

## 5. 中间件选择说明

### 5.1 使用的组件

| 组件 | 选择原因 | 替代方案 |
|-----|---------|---------|
| **SQLite** | 轻量级，零配置，开箱即用；适合中等量级 | PostgreSQL/MySQL（需要额外运维） |
| **Gorilla Mux** | 成熟的 Go HTTP 路由库，API 简洁 | 标准库 `net/http` + 自实现路由 |
| **go-sqlite3** | SQLite 的 Go 驱动，纯 Go 实现 | 其他 SQLite 驱动 |

---

## 6. API 文档

### 供应商管理

| 方法 | 路径 | 描述 |
|-----|------|------|
| POST | `/api/v1/providers` | 创建供应商 |
| GET | `/api/v1/providers` | 获取所有供应商 |
| GET | `/api/v1/providers/{id}` | 获取单个供应商 |
| PUT | `/api/v1/providers/{id}` | 更新供应商 |
| DELETE | `/api/v1/providers/{id}` | 删除供应商 |

### 通知管理

| 方法 | 路径 | 描述 |
|-----|------|------|
| POST | `/api/v1/notifications` | 创建通知 |
| GET | `/api/v1/notifications/{id}` | 获取单个通知 |
| POST | `/api/v1/notifications/{id}/retry` | 手动重试通知 |
| GET | `/api/v1/notifications/status/{status}` | 按状态查询通知 |

### 其他

| 方法 | 路径 | 描述 |
|-----|------|------|
| GET | `/health` | 健康检查 |
